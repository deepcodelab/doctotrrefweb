from django.urls import path
from . import views

app_name = 'live_stream'

urlpatterns = [
    # Add your live_stream HTTP routes here
    # Example: path('stream/', views.stream_view, name='stream'),
]
